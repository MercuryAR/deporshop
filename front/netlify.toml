[build]
  command = "npm run build"
  publish = "dist"

# React Router usa client-side routing - sin esto, refrescar /productos
# o /perfil directamente devuelve 404 de Netlify en vez de servir la SPA.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
